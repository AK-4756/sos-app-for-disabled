<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility Service configuration for VolumeButtonSosService.

    canRequestFilterKeyEvents = true  →  allows onKeyEvent() to receive
                                         hardware key presses including volume buttons.
    accessibilityEventTypes   = none  →  we subscribe to no UI events; the service
                                         uses key interception only, keeping its
                                         footprint minimal.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestFilterKeyEvents"
    android:canRequestFilterKeyEvents="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="100"
    android:settingsActivity="com.sosapp.disabled.SettingsActivity" />
